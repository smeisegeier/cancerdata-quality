[project]
name = "cancerdata-quality"
requires-python = ">= 3.11"
version = "0.8.0"
description = "repo for cancerdata quality"
readme = "README.md"
dependencies = [
    "pip",
    "pandas",
    "kaleido",
    "numpy",
    "duckdb>=1.5.0",
    "pandas-plots>=2.0.8",
    "connection-helper",
    "pygwalker",
    "appnope",
    "debugpy",
    "ipykernel",
    "jupyter-client",
    "nest-asyncio",
    "pyzmq",
    "tornado",
    "jupyter",
    "openpyxl>=3.1.5",
    "upsetty>=0.1.3",
    "upsetplot>=0.9.0",
    "ipylab>=1.0.0",
]

# [tool.setuptools.packages.find]
# where = ["src"]

[tool.uv]
package = true
